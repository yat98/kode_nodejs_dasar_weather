<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hello World</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300&family=Roboto+Condensed:wght@700&display=swap" rel="stylesheet"> 
  <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
  <div class="container">
    <fieldset>
      <form action="/" method="POST">
        <input type="text" id="city" name="city" placeholder="Enter a city" class="ghost-input" required>
        <input type="submit" value="Get Weather" class="ghost-button" id="get-weather">
      </form>
      <% if(weather !== null) { %>
        <p id="weather"><%= weather %></p>
      <% } %>

      <% if(error !== null) { %>
        <p id="error"><%= error %></p>
      <% } %>
    </fieldset>
  </div>
</body>
</html>